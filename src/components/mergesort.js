import { useState,useEffect } from "react";
import myObjects from "./global";

const Merge=(props)=>{
    const [merge,setMerge]=useState(myObjects)
    const [first,setFirst]=useState(0);

 

}

export default Merge;